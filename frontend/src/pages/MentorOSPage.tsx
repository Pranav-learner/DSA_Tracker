import { AIOperatingDashboard } from '@/components/ai';

/**
 * MentorOSPage — the AI Operating System dashboard (Module 7 · Sprint 4). The
 * high-level home for AI: today's brief, recommended workflows, the recommendation
 * center, the searchable timeline and quick actions. The AI recommends; the
 * learner decides.
 */
export function MentorOSPage() {
  return <AIOperatingDashboard />;
}
