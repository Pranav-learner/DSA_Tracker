import { makePlaceholderProvider } from './placeholder.js';

/** OllamaProvider — Sprint 1 placeholder (interface complete; local impl in a later sprint). */
export const ollamaProvider = makePlaceholderProvider('ollama', 'Placeholder — local Ollama integration lands in a later sprint.');
