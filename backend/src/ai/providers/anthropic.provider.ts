import { makePlaceholderProvider } from './placeholder.js';

/** AnthropicProvider — Sprint 1 placeholder (interface complete; impl in a later sprint). */
export const anthropicProvider = makePlaceholderProvider('anthropic', 'Placeholder — Anthropic integration lands in a later sprint.');
