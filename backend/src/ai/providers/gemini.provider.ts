import { makePlaceholderProvider } from './placeholder.js';

/** GeminiProvider — Sprint 1 placeholder (interface complete; impl in a later sprint). */
export const geminiProvider = makePlaceholderProvider('gemini', 'Placeholder — Google Gemini integration lands in a later sprint.');
